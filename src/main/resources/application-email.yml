spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 
    password: 
    default-encoding: UTF-8
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  application:
    mail:
      host: localhost
      http: http://
      confirmation: /guest/confirmation?token=
      reset-password: /guest/reset-password
      username: ?username=
      otp: otp
